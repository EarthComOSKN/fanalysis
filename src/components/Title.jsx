/** @jsx jsx */
import { css, jsx } from "@emotion/core";
import styled from "@emotion/styled";

export const Title = styled.div`
  color: #bcccef;
  font-size: 24px;
  margin: 16px 0;
  letter-spacing: 0.5;
`;
